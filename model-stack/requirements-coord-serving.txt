# Runtime deps for coord_serving HTTP image.
# Subset of pyproject.toml — only what the FastAPI app + teacher_proxy +
# tiered_voter + mock_cascade actually import at request time. Keep this
# list tight: every line shipped runs in production.
#
# When pyproject.toml's [project.dependencies] changes for any of these
# packages, bump the pin here too. Other model-stack deps (torch/vllm/
# transformers/peft/datasets) are explicitly NOT installed — they belong
# in a separate GPU image used by the training pipeline + REAL_ENSEMBLE.

# PINNED (2026-06-11, bumped 2026-07-20): exact versions to stop the pip resolver
# backtracking to exhaustion. The loose `>=` ranges resolved fine for months, but
# a new PyPI release made pip backtrack across httpx versions (all of which
# require httpcore==1.*) and bail with ResolutionImpossible, breaking the CI
# image build even though nothing in this file changed. These pins are validated
# to resolve cleanly in python:3.13-slim (pip install --dry-run, exit 0). Bump
# deliberately, and re-validate the whole set together, not one package at a time.
fastapi==0.139.2
uvicorn[standard]==0.51.0
httpx==0.28.1
httpcore==1.0.9
pydantic==2.13.4
pyyaml==6.0.3
prometheus-client==0.21.1
